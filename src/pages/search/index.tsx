import type { NextPage } from 'next';
import React from 'react';

import { Layout } from '@components/Layout';

const Search: NextPage = () => {
  return <Layout title="마이페이지">검색</Layout>;
};

export default Search;
